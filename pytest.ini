[pytest]
# Custom markers.  Registered here so that the opt-in end-to-end suite does not
# trigger PytestUnknownMarkWarning.  The unit suite uses no markers.
markers =
    e2e: end-to-end test against a real AWX instance (opt-in; skipped unless AWX_E2E_HOST and the awx CLI are available)
